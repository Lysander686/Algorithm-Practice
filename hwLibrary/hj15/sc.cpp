// n: n件物品
for (int i = 1; i <= n; i++)
{
    /**
     * w[i]: 每件物品的重量, 
     * v: 容量为v的背包,
     * V: 背包真实容量
     * */
    for (int v = w[i]; v <= V; v++)
    {
        // 状态转移方程: d[i][v]表示前i件物品恰好装入容量为v的背包中所能获得的最大价值
        dp[i][v] = max(dp[i - 1][v], dp[i - 1][v - w[i]])
    }
}